/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "kzmgd1foo8e4h8ujoaxe.lite.vusercontent.net"],
    unoptimized: true,
  },
}

module.exports = nextConfig

